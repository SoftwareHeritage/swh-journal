#!/usr/bin/make -f

export PYBUILD_NAME=swh.journal
export PYBUILD_TEST_ARGS=-m 'not db and not fs' -p swh.journal.pytest_plugin

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_install:
	dh_install
	rm -v $(CURDIR)/debian/python3-*/usr/lib/python*/dist-packages/swh/__init__.py
